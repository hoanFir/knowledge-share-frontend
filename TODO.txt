优化

发起、修改主题：
时间不一定要求在6小时之后？
有的人要马上举办活动呢？
解决：这就可以需要状态码判断了！

主题状态：
瀑布流：状态判断（报名中、已结束）

瀑布流：
判断参与人数，人数少于多少人会自动删除

6、判断主题是否1已经结束、2正在报名中
1结束条件（正常）：到达活动结束时间
前台需求：需要服务器时间（还是直接后台判断给我一个isEnded判断）
（不正常是手动被删除或者达不到条件删除，被删除的不做记录，后期做通知给用户说明被删除原因）

2报名中条件：用户点击发起开始到结束之间的时刻
前台需求：可以进行获取本地时间，假如相等，则为报名中（也可以还是之间后台给一个判断isProcessing）


【可能存在的问题】
瀑布流地点、title的显示没有省略号
实现单行和双行的省略
weui-media-box__title
weui-media-box__desc

【逻辑】
点击我的、查看瀑布流里的详情点击报名、点击参讲才需要获取个人信息
并且判断是否个人信息为空必须提示先填写个人信息才能报名
瀑布流获取不需要sid和进入的详情为普通用户。


【后期：暂时不做】

字符串转数值parseInt(arrTime[0] + 2)

1、后台提供瀑布流总数目total，在profile中显示

2、关键字实时搜索

3、 评论模块，包括message消息

4、状态码判断
自定义的statusCode放在返回的data中

5、按照最热最新最火来申请数据


【最佳实践】
1、fail修改：
如果有callback，值为Boolean类型的
      fail: (e) => {
        console.error("err" + e);
        callback(false);：
      }
如果有callback，值为对象等复杂类型的
        fail: (e) => console.error(e)
如果没有callback
      fail: (e) => {
        console.log(e)
      }