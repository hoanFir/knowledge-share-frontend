<!--index.wxml-->
<view class="container">
  <view class="userinfo">
    <!-- <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 进入微言 </button> -->
    <button open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 进入微言 </button>
    <!-- <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block> -->
  </view>
  <!-- <view class="usermotto">
    <button class="start" bindtap="handleStart">{{motto}}</button>
  </view> -->
</view>
